@model FinalProject.Models.BooksInventory

@{
    Layout = "_Layout";
    var title = "UPDATE BooksInventory";
    ViewData["Title"] = title;
}


<h4>User_Answers</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <!--<input type="hidden" asp-for="UserId" />
            <input type="hidden" asp-for="SurveyId" />-->
            <div class="form-group">
                <label id="Book_id" name="Book_id" class="control-label">Book_id</label>
                <input id="Book_id" name="Book_id" class="form-control" value="@Model.Book_id" disabled />
            </div>
            <div class="form-group">
                <label id="User_id" name="User_id" class="control-label">User_id</label>
                <input id="User_id" name="User_id" class="form-control" value="@Model.User_id" disabled />
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

